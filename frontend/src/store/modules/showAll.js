const state = {
  tasks: [{ id: 1, text: "hello will" }],
};
const getters = {
  allTasks: (state) => state.tasks,
};
const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
